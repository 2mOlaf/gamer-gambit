apiVersion: v1
kind: Service
metadata:
  name: jarvfjallet-service
  namespace: gamer-gambit
  labels:
    app: jarvfjallet
    component: discord-bot
spec:
  selector:
    app: jarvfjallet
    component: discord-bot
  ports:
  - name: health-check
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
